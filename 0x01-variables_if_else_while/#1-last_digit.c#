#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - The entry point for our code
 *
 * Return: Always return (0) value
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if(n > 5)
		printf("Last digit of %d is ", n)
		
	return (0);
}
